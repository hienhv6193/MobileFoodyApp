import React from 'react';
import { Text, View } from 'react-native';

const AddDiscount = ({
    params,
}) => (
    <View>
        <Text>AddDiscount</Text>
    </View>
);

export default AddDiscount;
