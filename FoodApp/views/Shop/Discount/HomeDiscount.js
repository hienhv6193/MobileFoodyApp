import React from 'react';
import { Text, View } from 'react-native';

const HomeDiscount = ({
    params,
}) => (
    <View>
        <Text>HomeDiscount</Text>
    </View>
);

export default HomeDiscount;
