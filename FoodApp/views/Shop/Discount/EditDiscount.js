import React from 'react';
import { Text, View } from 'react-native';

const EditDiscount = ({
    params,
}) => (
    <View>
        <Text>EditDiscount</Text>
    </View>
);

export default EditDiscount;
